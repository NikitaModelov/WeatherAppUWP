<Page
    x:Class="WeatherAppUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WeatherAppUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel>

            <TextBox Text="{x:Bind ViewModel.City, Mode=TwoWay}"/>

            <ProgressRing x:Name="progressRing" IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Width="60" Height="60" />

            <TextBlock Foreground="White" 
                   x:Name="textBlock1"
                Height="75" Width="125"
                HorizontalAlignment="Center"
                Text="{x:Bind ViewModel.CurrentTemperature.Temp.ToString(), Mode=OneWay}"/>

            <Button Content="Обновить" Command="{x:Bind ViewModel.FetchCurrentWeather, Mode=OneWay}"/>

        </StackPanel>
    </Grid>
</Page>
